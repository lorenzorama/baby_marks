name: Deploy

permissions:
  contents: read

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: baby
          POSTGRES_PASSWORD: baby
          POSTGRES_DB: baby_marks
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U baby -d baby_marks"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4

      # ---- Frontend ----
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Frontend deps
        working-directory: frontend
        run: npm ci
      - name: Frontend tests
        working-directory: frontend
        run: npx vitest run
      - name: Frontend typecheck
        working-directory: frontend
        run: npx tsc --noEmit

      # ---- Backend (incl. DB integration tests) ----
      - uses: astral-sh/setup-uv@v5
      - name: Backend deps
        working-directory: backend
        run: uv sync
      - name: Backend tests
        working-directory: backend
        env:
          BM_TEST_DATABASE_URL: postgresql://baby:baby@localhost:5432/baby_marks
        run: |
          uv run pytest -ra 2>&1 | tee pytest-output.txt
          ! grep -q "BM_TEST_DATABASE_URL not set" pytest-output.txt

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Deploy over SSH
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          cp deploy/known_hosts ~/.ssh/known_hosts
          ssh -i ~/.ssh/deploy_key -o ConnectTimeout=10 -o BatchMode=yes -o StrictHostKeyChecking=yes "$VPS_USER@$VPS_HOST" "DEPLOY_SHA=$GITHUB_SHA bash -s" < deploy/remote-deploy.sh

      - name: Health check
        run: |
          for i in $(seq 1 18); do
            if curl -fsS --max-time 5 https://baby.canastrat.com/api/health > /dev/null \
               && curl -fsS --max-time 5 https://baby.canastrat.com/.well-known/oauth-authorization-server > /dev/null; then
              echo "Healthy."
              exit 0
            fi
            echo "Not healthy yet (attempt $i/18), retrying in 5s..."
            sleep 5
          done
          echo "Health check FAILED after 90s — check VPS logs; pre-deploy backup is in ~/backups on the VPS."
          exit 1
